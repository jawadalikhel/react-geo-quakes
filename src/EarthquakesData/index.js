import React, {Component} from 'react';

class EarthquakesData extends Component{
  render(){
    const earthquakesList = this.props.earthquackes.map((earthquacke, i) =>{

    })
    return(
      <div>
      </div>
    )
  }
}
